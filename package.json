{
  "name": "explicitjs",
  "version": "1beta3",
  "private": true,
  "type": "module",
  "description": "A semantic clarity enforcer for production JavaScript & TypeScript.",
  "license": "MIT",
  "bin": {
    "explicitjs": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "tsx src/cli.ts",
    "lint": "tsx src/cli.ts .",
    "fmt": "biome check --write .",
    "fmt:check": "biome ci .",
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json && sed -i.bak '1s|.*|#!/usr/bin/env node|' dist/cli.js && rm -f dist/cli.js.bak && chmod +x dist/cli.js",
    "prepack": "npm run build"
  },
  "dependencies": {
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.8",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0"
  }
}
